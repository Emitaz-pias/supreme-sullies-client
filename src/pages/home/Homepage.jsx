import React from 'react';
import Layout from '../../components/layout/Layout';

const Homepage = () => {
    return (
       <Layout>
        <div style={{height:'120vh'}}>here is the home page contents</div>
       </Layout>
    );
};

export default Homepage;